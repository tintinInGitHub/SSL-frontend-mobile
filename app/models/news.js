class News {
  constructor(id, title, color, video_url, banner, description) {
    this.id = id;
    this.title = title;
    this.color = color;
    this.video_url = video_url;
    this.banner = banner;
    this.description = description;
  }
}

export default News;
